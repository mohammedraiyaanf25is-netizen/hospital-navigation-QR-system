const directions = {
  opd: "From main entrance: go straight 15 meters → take lift to 1st floor → OPD is on left.",
  pharmacy: "From main entrance: walk 10 meters → turn left → Pharmacy is the first counter on right.",
  lab: "From main entrance: go straight → turn right at corridor → Lab is next to X-Ray room.",
  doctors: "From OPD waiting area: follow the green line on the floor → Consulting rooms ahead."
};

function loadDirections(dept) {
    document.getElementById("directions_text").innerText = directions[dept];
}

function speakDirections(dept) {
    speak(directions[dept]);
}